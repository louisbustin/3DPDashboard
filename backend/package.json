{
	"scripts": {
		"start": "nodemon --exec sam build",
		"localapi": "sam local start-api -p 3001"
	},
	"dependencies": {
		"@aws-sdk/client-dynamodb": "^3.427.0",
		"@aws-sdk/lib-dynamodb": "^3.427.0",
		"jsonwebtoken": "^9.0.2",
		"uuid": "^9.0.1"
	},
	"devDependencies": {
		"nodemon": "^3.0.1"
	}
}
